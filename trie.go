package substring

/*
type Node struct{
	Children map[rune]Node
}
func longestSubTrie(s string) int {
	root := new(Node)
	for _, c := range s {

	}
}

func (n *Node) addRune(r rune, rest string) {
	child, ok := n.Children[r]
	if ok {
		if rest == "" {
			child.Children = nil
		} else {
			child.Children = make(map[rune]Node)
			child.addRune(strings.Slice(ddrest, 1)
		}
	} else {
		child := new(Node)
		n.Children[r] := child
	}
}
*/
